import * as modes from './sagaModes';

export const sagaModes = { ...modes };

export { default as routes } from './routes';
export { default as tabs } from './tabs';

export default {
  appTitle: 'Portfolio: Admin Panel',
};
